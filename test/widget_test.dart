import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test — just verifies app boots without crash
    // Full integration tests require device + location + AdMob setup
  });
}
